interface:
  display_name: "Linx ISA"
  short_description: "ISA architecture and specification guidance"
  default_prompt: "Use $linx-isa to close LinxArch contract gates, keep architecture pages authoritative, and map each required gate to a documented ISA/µarch contract."
