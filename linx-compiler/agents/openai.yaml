interface:
  display_name: "Linx Compiler"
  short_description: "LLVM backend and compile gate workflow"
  default_prompt: "Use $linx-compiler to implement and validate Linx compiler backend changes and call/ret compile conformance."
